apiVersion: v1
kind: Service
metadata:
  name: java-student-service
  labels:
    app: java-student-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: java-student-app